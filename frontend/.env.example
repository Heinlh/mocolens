# Backend API base URL. Empty/unset = the app runs entirely on mock data
# (frontend/src/data/mock/) with no network calls, per the frontend's
# original design goal of running standalone.
VITE_API_BASE_URL=http://localhost:8001
